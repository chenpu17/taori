# @chenpu17/taori

Standalone Taori sidecar CLI.

## Install

```bash
npm install -g @chenpu17/taori
```

## Usage

```bash
taori --port 17890
```

Optional flags:

- `--port <number>` set the listening port
- `--db-path <path>` override the sqlite database path
